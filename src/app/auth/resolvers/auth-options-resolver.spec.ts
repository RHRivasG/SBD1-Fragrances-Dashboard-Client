import { AuthOptionsResolver } from './auth-options-resolver';

describe('AuthOptionsResolver', () => {
  it('should create an instance', () => {
    //expect(new AuthOptionsResolver()).toBeTruthy();
  });
});
